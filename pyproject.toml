[project]
name = "cows-and-bulls"
version = "0.1.0"
description = "Cows & Bulls number-guessing game built with Streamlit"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.55.0",
]
